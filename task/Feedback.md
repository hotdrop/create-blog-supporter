# 2026/5/31 09:59 フィードバック

## 作業内容
- 記事編集画面からタイトル直接編集を分離し、記事タイトル編集専用画面を追加した。
- Markdown出力由来のメッセージをMarkdown出力ボタン直下に表示するように変更した。

## 開発改善フィードバック
- 既存ルール・手順が障壁になった点: Route/Screen/ViewModelの分離ルールが明確だったため大きな障壁はなかったが、既存のタイトル編集責務がArticleEditorに混在していたため、画面分離時の確認対象がUI、ViewModel、ナビゲーションにまたがった。
- 改善した方がよいルール・手順: 画面から機能を分離するタスクでは、既存画面のcallback、UiState、message sealed interface、Preview、stringsを横断検索する手順をチェック項目として明示すると漏れを減らせる。
- 追加した方がよいルール・手順: Navigation Composeで既存の分岐Route配下に新画面を追加する場合、親Routeのcallback追加とAppDestination追加を同時に確認するチェックを用意するとよい。
- docs/README/タスクメモ/テストなどへ反映した方がよい点: 今回は既存UseCaseのテストで保存ルールを担保でき、ViewModel固有の複雑なロジック追加は小さいため追加テストは不要と判断した。UI遷移は手動確認観点としてタスク記録に残すとよい。

## 分類
- タスク固有: 記事編集画面からタイトル編集画面を分離する際の横断確認ポイント。
- 恒久対応候補: 画面分離・機能移動時の検索チェックリストをCompose UIスキルに追加するか検討。

## 更新先候補
- AGENTS.md: なし。恒久ルールに追加するほどのドメイン制約変更はない。
- .codex/skills/md-doc-viewer/SKILL.md: なし。今回の作業対象外。
- docs/README/task/tests など: タイトル編集画面の手動確認観点を今後のUI検証メモに残す余地がある。

# 2026/5/31 10:25 フィードバック

## 作業内容
- 章節本文編集画面から保存済み本文表示と「保存済みに戻す」ボタンを削除した。
- 未保存の編集中本文がある状態で戻る場合に、編集破棄確認ダイアログを表示し、確定時にdraftContentを保存済み本文へ戻してから閉じるようにした。

## 開発改善フィードバック
- 既存ルール・手順が障壁になった点: UI上のリセットボタン削除と、戻る時の破棄処理で同じUseCaseを内部利用する要件が近く、単純な機能削除ではなく導線変更として扱う必要があった。
- 改善した方がよいルール・手順: 「保存せずに戻る」「破棄して閉じる」のような文言がある場合、draftContentの自動保存仕様と矛盾しないよう、画面遷移時にdraftContentをどう扱うかを実装前チェックに含めるとよい。
- 追加した方がよいルール・手順: Composeで戻るジェスチャー対策を入れる場合、TopAppBarの戻るアイコン、システムBackHandler、ダイアログ表示中の戻る挙動を同じ確認セットで見る手順を追加するとよい。
- docs/README/タスクメモ/テストなどへ反映した方がよい点: Kotlin/Gradleの検証コマンドを同時に走らせるとKotlin daemonのインクリメンタルキャッシュ競合が起きうるため、このプロジェクトではGradle検証を順番に実行する運用メモがあるとよい。

## 分類
- タスク固有: 章節編集画面の戻る破棄確認と保存済み本文表示削除に関する確認観点。
- 恒久対応候補: draftContent自動保存と画面離脱時破棄の関係、BackHandler導入時の確認観点、Gradle検証の逐次実行方針。

## 更新先候補
- AGENTS.md: なし。既存のcontent/draftContent恒久ルールの範囲内。
- .codex/skills/md-doc-viewer/SKILL.md: なし。今回の作業対象外。
- docs/README/task/tests など: Gradle検証コマンドは並列ではなく逐次実行する旨を開発メモに反映する余地がある。

# 2026/5/31 10:59 フィードバック

## 作業内容
- 章節編集画面の「ローカルLLMに相談」を、ChatGPTに貼り付ける章本文たたき台依頼文の生成機能へ変更した。
- 章節相談からローカルLLM呼び出し、モデル未設定、生成中、中断の状態を外し、貼り付け用依頼文のUseCaseとテストへ置き換えた。

## 開発改善フィードバック
- 既存ルール・手順が障壁になった点: LLM相談ルールが「助言・提案」寄りに定義されていたため、今回のように外部AIへ本文たたき台作成を依頼しつつアプリには自動反映しない設計との線引きを明確にする必要があった。
- 改善した方がよいルール・手順: 外部AIへ貼り付けるプロンプト生成は外部API連携ではないが、ユーザーが別アプリへ渡す文章であるため、出力内容と自動反映禁止の確認観点を記事ドメインスキルに追加するとよい。
- 追加した方がよいルール・手順: ローカルLLM機能を別方式へ置換する場合、ViewModelのJob、loading/cancel/error message、strings、Preview、UseCaseテストを横断検索するチェックを追加すると漏れを減らせる。
- docs/README/タスクメモ/テストなどへ反映した方がよい点: ChatGPT貼り付け用依頼文の期待トーンと禁止文言は、将来のプロンプト変更時に壊れやすいためUseCaseテストで維持するのが適切。

## 分類
- タスク固有: 章節相談をChatGPT貼り付け用依頼文生成へ置換する際の確認観点。
- 恒久対応候補: 外部AIへ貼る文章生成と、アプリ内LLM出力の自動反映禁止を両立するルールの明文化。

## 更新先候補
- AGENTS.md: なし。恒久ルールの「外部連携追加禁止」と「LLM出力を保存済み本文へ自動反映しない」の範囲内。
- .codex/skills/article-writing-domain/SKILL.md: ChatGPT貼り付け用依頼文生成の扱いと、本文たたき台依頼を許容する条件を追記する候補。
- docs/README/task/tests など: 貼り付け用依頼文の期待文言をUseCaseテストに残したため、追加ドキュメントは現時点では不要。
