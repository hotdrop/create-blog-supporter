# 2026/5/24 19:58 フィードバック

## 作業内容
- Task6として、LiteRT-LM本体接続前のLLM支援境界をdomain層に追加した。
- 既存の目次案スタブを用途別のFake UseCaseへ置き換え、章節概要・改善提案・誤字脱字チェックのFake UseCaseを追加した。
- 既存ユニットテストを新境界向けに更新し、`./gradlew :app:testDebugUnitTest` で検証した。

## 開発改善フィードバック
- 既存ルール・手順が障壁になった点: Task6の範囲が「境界のみ」か「章節UIまで含む」かはタスク本文だけでは曖昧だったため、事前計画で明確化してから実装する必要があった。
- 改善した方がよいルール・手順: LLM支援機能はUI連携タスクと境界タスクを分ける方針を、NextTaskや設計メモに明記すると実装範囲がぶれにくい。
- 追加した方がよいルール・手順: Task7でSDK接続を行う前に、今回追加したFake UseCaseをどの抽象境界へ差し替えるかを短く設計しておくとよい。
- docs/README/タスクメモ/テストなどへ反映した方がよい点: `docs/DesignDocument.md` のLiteRT-LM表記とTask単位の実装境界は、AGENTS.mdの恒久ルールと同じ表記へ揃える余地がある。

## 分類
- タスク固有: Task6では章節編集画面の新UI追加を行わず、Fake UseCaseと既存目次案フロー差し替えに限定した判断。
- 恒久対応候補: LLM境界実装では、SDK型をUI層へ出さないだけでなく、提案採用時の保存先を `draftContent` に限定する観点をテスト観点として維持する。

## 更新先候補
- AGENTS.md: 既存ルールで十分だが、将来のTask7以降でLLM client抽象の置き場所を恒久化するなら追記候補。
- .codex/skills/md-doc-viewer/SKILL.md: 該当なし。
- docs/README/task/tests など: `task/NextTask.md` のTask7に「Fake UseCaseをSDK境界へ接続する」旨を追記すると次工程が明確になる。

# 2026/5/24 20:08 フィードバック

## 作業内容
- Task7として、LiteRT-LM SDKをdata層のclient実装へ接続し、UseCaseからアプリ独自型経由で呼び出す構成へ差し替えた。
- Engine再利用、Flowストリーミング、キャンセル再送出、GPU初期化失敗時のCPU fallback、モデル不在・初期化失敗・生成失敗・パース失敗の変換を追加した。
- Fake clientを使うUseCaseテストへ更新し、`./gradlew :app:compileDebugKotlin`、`./gradlew :app:testDebugUnitTest`、`./gradlew :app:assembleDebug` で検証した。

## 開発改善フィードバック
- 既存ルール・手順が障壁になった点: モデルファイルの配置場所とユーザー導線がTask7本文だけでは未定義だったため、実装では `filesDir/models/blog-supporter.litertlm` の規約に限定した。
- 改善した方がよいルール・手順: LiteRT-LMモデルの配置パス、初回セットアップ手順、モデル未配置時のUI文言をタスクまたはREADMEに明記した方がよい。
- 追加した方がよいルール・手順: LLM応答の構造化フォーマットはプロンプトとパーサーが強く結合するため、今後のUI連携前にパース失敗時の再試行やフォールバック方針を決めておくとよい。
- docs/README/タスクメモ/テストなどへ反映した方がよい点: `docs/LocalLLMSample/` を参照した実装ではCPU fallbackとnative library宣言が必要になるため、ローカルLLMセットアップメモとして残す価値がある。

## 分類
- タスク固有: Task7ではSDK接続とUseCase差し替えに限定し、モデルダウンロードや外部連携、設定画面追加はMVP非スコープとして扱った。
- 恒久対応候補: LiteRT-LMモデル配置規約とエラー表示方針は今後のLLM UI実装でも再利用されるため、恒久的な開発メモ候補。

## 更新先候補
- AGENTS.md: モデル配置規約を恒久ルールにするなら追記候補。
- .codex/skills/md-doc-viewer/SKILL.md: 該当なし。
- docs/README/task/tests など: READMEまたはdocsに `filesDir/models/blog-supporter.litertlm` の配置手順、モデル未配置時の期待動作、LLM応答フォーマットを追記するとよい。

# 2026/5/29 12:30 フィードバック

## 作業内容
- アプリ全体を端末設定に関係なくダークテーマ固定にした。
- ComposeのColorScheme、起動時Windowテーマ、StatusBar/NavigationBarのSystem UIスタイルを暗色前提へ揃えた。
- `./gradlew :app:compileDebugKotlin`、`./gradlew :app:testDebugUnitTest`、`./gradlew :app:assembleDebug` で検証した。

## 開発改善フィードバック
- 既存ルール・手順が障壁になった点: ダーク固定時のSystem Bar方針が既存設計に明記されておらず、ComposeテーマとXMLテーマの両方を確認して判断する必要があった。
- 改善した方がよいルール・手順: UIテーマ変更では、Compose ColorSchemeと起動直後のXMLテーマ、System Barアイコン明暗を同時に確認する手順を残すとよい。
- 追加した方がよいルール・手順: 個人利用アプリとしてライト/ダーク追従を不要にする場合は、端末設定に追従しない固定テーマであることをREADMEや設計メモに明記しておくとよい。
- docs/README/タスクメモ/テストなどへ反映した方がよい点: 実機確認項目として「ライトモード端末でもアプリは暗色」「StatusBar/NavigationBarのアイコンが見える」をUI検証メモに追加する価値がある。

## 分類
- タスク固有: 今回は保存ロジックや画面構造を変更せず、テーマとSystem Bar表示だけに限定した。
- 恒久対応候補: このアプリをダーク固定で運用する方針は、今後のUI追加時にも色選定やPreview確認の前提になる。

## 更新先候補
- AGENTS.md: ダーク固定を恒久ルールにするなら追記候補。
- .codex/skills/md-doc-viewer/SKILL.md: 該当なし。
- docs/README/task/tests など: READMEまたはUI検証メモにSystem Bar表示確認の観点を追記するとよい。

# 2026/5/29 12:58 フィードバック

## 作業内容
- LiteRT-LMモデル設定画面を追加し、トップ画面右上の設定アイコンから遷移できるようにした。
- `.litertlm` ファイルをローカルストレージから選択し、アプリ内ストレージへコピーして保存パスをSharedPreferencesへ保持する設定Repositoryを追加した。
- LiteRT-LM clientのモデルパス解決を固定パスから保存済み設定へ変更し、未設定・ファイル欠落時のユーザー向けメッセージを分けた。
- `./gradlew :app:compileDebugKotlin`、`./gradlew :app:testDebugUnitTest` で検証した。

## 開発改善フィードバック
- 既存ルール・手順が障壁になった点: LiteRT-LMモデル設定の保存先、ファイルコピー方式、未設定時の既存導線維持はタスク計画で確定できたが、今後もLLM機能追加前に初期設定導線を先に確認する必要がある。
- 改善した方がよいルール・手順: Gradle検証を並列実行するとKotlin増分コンパイルキャッシュが競合したため、同一variantを触る `compileDebugKotlin` と `testDebugUnitTest` は直列実行する手順がよい。
- 追加した方がよいルール・手順: LiteRT-LMモデル選択は権限追加なしで `OpenDocument` からアプリ内コピーする方針を、READMEまたは開発メモへ残すと次回以降の判断が安定する。
- docs/README/タスクメモ/テストなどへ反映した方がよい点: 初回起動後に設定画面で `.litertlm` を選択してから目次案生成する手順をREADMEに追記すると実機確認がしやすい。

## 分類
- タスク固有: 今回はモデルファイル設定のみを扱い、プロンプト設定や外部モデル取得、権限追加は行わなかった。
- 恒久対応候補: LLM未設定時もLLM不要の執筆メモ作成はブロックしない方針は、今後の支援機能追加でも維持候補。

## 更新先候補
- AGENTS.md: LiteRT-LMモデル設定導線や未設定時の非LLM機能維持を恒久化するなら追記候補。
- .codex/skills/md-doc-viewer/SKILL.md: 該当なし。
- docs/README/task/tests など: READMEに初回モデル設定手順、テストメモにGradle直列実行の注意を追記するとよい。

# 2026/5/29 13:08 フィードバック

## 作業内容
- LiteRT-LMのタイトル案・目次案生成で、LLM応答の形式ゆれにより候補表示まで進めない問題を修正した。
- 厳密な `TITLE:` / `OUTLINE:` 形式だけでなく、番号付き行や通常行から候補を抽出し、足りない場合は題材メモから採用前候補を補完するフォールバックを追加した。
- ローカルLLM向けに「時間をおいて再度」の文言を廃止し、モデル未設定・初期化失敗・応答取得失敗の表示を分けた。
- `./gradlew :app:compileDebugKotlin`、`./gradlew :app:testDebugUnitTest` で検証した。

## 開発改善フィードバック
- 既存ルール・手順が障壁になった点: LLM出力形式を厳密に要求してもローカルモデルが必ず従うとは限らず、パーサー失敗がユーザーには生成失敗に見えていた。
- 改善した方がよいルール・手順: ユーザー操作を止める画面では、LLM応答の構造化パースに失敗しても候補表示へ進める復旧策を標準にした方がよい。
- 追加した方がよいルール・手順: ローカルLLMのエラー文言では「時間をおいて」などクラウドサービス前提の表現を避け、モデル設定・初期化・応答取得のどこで失敗したかを分ける。
- docs/README/タスクメモ/テストなどへ反映した方がよい点: LLM応答パーサーには、厳密形式、番号付き形式、自由記述、空応答の復旧テストを追加する方針をテストメモへ残すとよい。

## 分類
- タスク固有: 今回は目次案生成フローの候補表示復旧と文言修正に限定し、SDK呼び出し方式やモデル設定導線は変更しなかった。
- 恒久対応候補: LLMの出力形式ゆれを前提に、UseCase層でUIに渡せるアプリ独自候補へ復旧する方針。

## 更新先候補
- AGENTS.md: ローカルLLM向けエラー文言とパース失敗時フォールバックを恒久ルール化するなら追記候補。
- .codex/skills/md-doc-viewer/SKILL.md: 該当なし。
- docs/README/task/tests など: LLM応答復旧方針とユーザー向けエラー文言の設計メモを追加するとよい。

# 2026/5/29 13:18 フィードバック

## 作業内容
- 目次案生成中のLoading画面を、スピナーのみの表示からローカルLLM処理中であることが分かる待機画面へ変更した。
- 数分かかる場合があること、端末内処理で通信待ちではないこと、戻ると生成を中断することを表示した。
- 生成中画面に進捗バー、処理ステップ、キャンセル導線、UI検証用testTagを追加した。
- `./gradlew :app:compileDebugKotlin`、`./gradlew :app:testDebugUnitTest` で検証した。

## 開発改善フィードバック
- 既存ルール・手順が障壁になった点: ローカルLLMの初回生成が数分かかる前提がUI仕様に反映されておらず、空のLoading表示ではユーザーが停止や異常と判断しやすかった。
- 改善した方がよいルール・手順: 30秒を超える可能性がある処理では、単なるインジケータではなく処理内容、所要時間の目安、中断方法を画面内に表示する方針がよい。
- 追加した方がよいルール・手順: ローカルLLM処理中の表示は「通信待ち」ではなく「端末内でモデル処理中」と明示する。
- docs/README/タスクメモ/テストなどへ反映した方がよい点: 実機確認項目に、目次案生成中画面の長時間表示、戻る操作で生成が中断されることを追加するとよい。

## 分類
- タスク固有: 今回は目次案生成中の待機UIのみを改善し、生成処理や保存ルールは変更しなかった。
- 恒久対応候補: 長時間ローカル処理では、待機理由と中断導線を表示するUI方針。

## 更新先候補
- AGENTS.md: 長時間ローカル処理の待機UI方針を恒久化するなら追記候補。
- .codex/skills/md-doc-viewer/SKILL.md: 該当なし。
- docs/README/task/tests など: UI検証メモに長時間Loading表示とキャンセル導線の確認を追記するとよい。

# 2026/5/29 13:22 フィードバック

## 作業内容
- 章節本文編集画面で入力中にちらついていた自動保存中ラベルを非表示にした。
- 自動保存完了メッセージも画面表示から外し、入力中にステータス行やメッセージ領域が出入りしないようにした。
- 自動保存処理自体と `draftContent` / `content` 分離ルールは変更していない。
- `./gradlew :app:compileDebugKotlin`、`./gradlew :app:testDebugUnitTest` で検証した。

## 開発改善フィードバック
- 既存ルール・手順が障壁になった点: 自動保存状態をユーザーへ逐次表示する仕様が、500ms単位の状態更新と組み合わさって入力中のUIカクつきにつながっていた。
- 改善した方がよいルール・手順: 高頻度に変わる内部状態は、本文入力欄近くでレイアウトを増減させる表示に使わない方がよい。
- 追加した方がよいルール・手順: 自動保存は必要でも、成功通知は常時表示せず、失敗時や明示保存時だけメッセージを出す方針がよい。
- docs/README/タスクメモ/テストなどへ反映した方がよい点: 章節編集の実機確認に「入力中にステータス行がちらつかないこと」を追加するとよい。

## 分類
- タスク固有: 今回は画面表示のみ変更し、自動保存のUseCaseや保存タイミングは維持した。
- 恒久対応候補: 高頻度状態更新をUIの表示追加・削除に直結させないCompose設計方針。

## 更新先候補
- AGENTS.md: 高頻度状態更新の表示方針を恒久化するなら追記候補。
- .codex/skills/md-doc-viewer/SKILL.md: 該当なし。
- docs/README/task/tests など: UI検証メモに入力中のちらつき確認を追記するとよい。

# 2026/5/29 19:25 フィードバック

## 作業内容
- 記事編集画面下部のインライン全文プレビューを削除し、全文プレビューボタンから別画面へ遷移する構成にした。
- 新しい全文プレビュー画面を追加し、保存済みタイトルと各章節の保存済み `content` のみを表示するようにした。
- `draftContent` と未保存タイトル入力をプレビューに混ぜない方針を維持した。
- `./gradlew :app:compileDebugKotlin`、`./gradlew :app:testDebugUnitTest` で検証した。

## 開発改善フィードバック
- 既存ルール・手順が障壁になった点: 記事編集画面に全文プレビューを直接置くと、本文量が増えたときに編集操作と確認操作が同じ縦スクロール内で干渉しやすい。
- 改善した方がよいルール・手順: 長文確認は編集フォームと分離し、別画面または明示的な表示切り替えにした方が操作性を保ちやすい。
- 追加した方がよいルール・手順: プレビュー画面でもMarkdown出力ルールと同じく保存済み `content` を優先し、未保存の `draftContent` を混ぜないことを維持する。
- docs/README/タスクメモ/テストなどへ反映した方がよい点: UI検証メモに「記事編集画面で長文プレビューがインライン表示されないこと」「別画面プレビューが保存済み本文だけを表示すること」を追加するとよい。

## 分類
- タスク固有: 今回は表示導線の分離のみで、Markdown出力や共有処理は変更していない。
- 恒久対応候補: 長文プレビューは編集画面へ直接埋め込まず、保存済み本文確認用の独立画面にする方針。

## 更新先候補
- AGENTS.md: 保存済み本文プレビューの扱いを恒久化するなら追記候補。
- .codex/skills/md-doc-viewer/SKILL.md: 該当なし。
- docs/README/task/tests など: UI検証メモに全文プレビュー別画面化の確認観点を追記するとよい。

# 2026/5/29 19:37 フィードバック

## 作業内容
- 記事編集画面の章節カードにある確認状態 `AssistChip` の表示を調整した。
- 確認済みはチェックアイコン付きで青系の文字色とBorder、未確認はアイコンなしで赤系の文字色とBorderにした。
- 章節カードのクリック動作や確認状態のデータ更新処理は変更していない。
- `./gradlew :app:compileDebugKotlin`、`./gradlew :app:testDebugUnitTest` で検証した。

## 開発改善フィードバック
- 既存ルール・手順が障壁になった点: 状態チップの視覚仕様が状態ごとに明確化されていなかったため、確認済み/未確認の差が文字列だけに寄っていた。
- 改善した方がよいルール・手順: 重要な確認状態は文字列だけでなく、色・アイコン・枠線など複数の視覚手がかりを使う方針がよい。
- 追加した方がよいルール・手順: 未確認など注意が必要な状態はerror color、完了状態はprimary colorなど、MaterialThemeの意味色に寄せて実装する。
- docs/README/タスクメモ/テストなどへ反映した方がよい点: UI検証メモに章節カードの確認済み/未確認チップ表示を追加するとよい。

## 分類
- タスク固有: 今回は表示だけを変更し、確認済み状態の保存ロジックは変更していない。
- 恒久対応候補: 記事の確認状態を表すUIは、状態ごとに一貫した色とアイコンルールを持たせる方針。

## 更新先候補
- AGENTS.md: 状態表示チップの色・アイコン方針を恒久化するなら追記候補。
- .codex/skills/md-doc-viewer/SKILL.md: 該当なし。
- docs/README/task/tests など: UI検証メモに状態チップの見た目確認を追記するとよい。
